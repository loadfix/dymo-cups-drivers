
# Temporary
*.log
*.bck
*.bak
*.tmp
.*.swp
*~

# IDE
.vscode
.project
.cproject
.settings
.metadata

# Generated by GNU Autoconf
# (Note: AUTHORS/ChangeLog/INSTALL/NEWS/README are now committed files —
# taken verbatim from the 1.4.0.5 upstream tarball — because GNU automake
# requires them and we want them under version control so that both
# `./build.sh` and `dpkg-buildpackage` work without hacks.)
COPYING
autom4te.cache
aclocal.m4
.deps
.dirstamp
Makefile.in
Makefile
config.h.in
config.h
config.log
config.status
configure.scan
configure
compile
depcomp
test-driver

# Debian packaging build artifacts
debian/.debhelper/
debian/autoreconf.after
debian/autoreconf.before
debian/debhelper-build-stamp
debian/files
debian/printer-driver-dymo/
debian/printer-driver-dymo.substvars
.pc/

# Compilation artifacts
*.o
*.a
*.elf
*.bin
*.so
*.lo
raster2dymolm
raster2dymolw
raster2dymolm_v2
raster2dymolw_v2
stamp-h1
stamp-h2
